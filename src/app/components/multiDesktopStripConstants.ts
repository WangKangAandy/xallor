/** 与 `MultiDesktopStrip` 横滑 `transitionDuration` 一致；改动画时长时请同步。 */
export const DESKTOP_SLIDE_MS = 300;

export const WHEEL_COOLDOWN_MS = 450;
export const WHEEL_DELTA_MIN = 8;

/** 页码指示条自动隐藏延迟（切页或拒绝新建时每次重置计时） */
export const DOTS_AUTO_HIDE_MS = 800;
